<div class="container">
  <div class="row">
    <div class="col-6">
      <div><%= @list.name %>:</div>
      bookmarks are
      <div><%= @list.bookmarks.first.comment %></div>
      <div><%= @list.bookmarks.first.movie.title %></div>
      <div>

        <%= link_to "add new bookmarkt", new_list_bookmark_path(@list) %>
      </div>
    </div>
  </div>
</div>
